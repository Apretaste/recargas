<div class="row">
	<div class="col s12">
		<h1>Últimos canjes</h1>
		<p>Felicitamos todos nuestros usuarios que han conseguido el crédito y canjeado por recargas. Para que todos les conozcan, listamos los últimos veinte a continuación.</p>

		<table class="table striped centered">
			<thead>
				<tr>
					<th>Usuario</th>
					<th>Fecha</th>
				</tr>
			</thead>
			<tbody>
				<% recharges.forEach(function(recharge) { %> 
				<tr>
					<td><a href="#!" onclick="apretaste.send({'command':'PERFIL','data':{'username':'<%= recharge.username %>'}})">@<%= recharge.username %></a></td>
					<td><%= recharge.inserted %></td>
				</tr>
				<% }) %>
			</tbody>
		</table>
	</div>
</div>

<div class="bottom-space"></div>

<!-- GO BACK -->
<div class="fixed-action-btn">
	<a class="btn-floating btn-large waves-effect waves-light green" href="#!" onclick="apretaste.send({'command':'RECARGAS'})"><i class="material-icons">arrow_back</i></a>
</div>